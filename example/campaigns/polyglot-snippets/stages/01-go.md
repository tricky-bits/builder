---
title: Snippet One
author: Ada
slug: snippet-go
difficulty: 1
eta_minutes: 2
start: true
next: snippet-python
tags:
  - go
  - systems
answer: "go"
completion_message: "Correct — the `package`/`func main` shape gives it away."
hints:
  - wait_seconds: 0
    text: "Look at the `package main` line and the `:=` operator."
  - wait_seconds: 20
    text: "Its mascot is a gopher."
---

Name the language used in the snippet below.

```go
package main

import "fmt"

func main() {
    greeting := "Hello, world!"
    fmt.Println(greeting)
}
```

### Clues

- A `package` declaration at the top, then imports.
- Short variable declaration with `:=`.
- `func main()` is the entry point.

The language is **go**.

Type the answer: `go` (lowercase).
