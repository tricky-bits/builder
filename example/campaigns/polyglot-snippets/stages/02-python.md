---
title: Snippet Two
author: Ada
slug: snippet-python
difficulty: 1
eta_minutes: 2
next: snippet-sql
tags:
  - python
  - dynamic
answer: "python"
completion_message: "Right — indentation-defined blocks and f-strings."
hints:
  - wait_seconds: 0
    text: "Indentation defines the blocks; there are no braces."
  - wait_seconds: 20
    text: "Named after a British comedy troupe, not the snake."
---

Name the language used in the snippet below.

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

if __name__ == "__main__":
    print(greet("world"))
```

### Clues

- Indentation defines blocks — no curly braces.
- `def` introduces a function; type hints use `->`.
- f-strings interpolate with `{ }`.

The language is **python**.

Type the answer: `python` (lowercase).
